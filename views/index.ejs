<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Inventory Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans min-h-screen">
  <header class="bg-indigo-600 text-white shadow">
    <div class="max-w-6xl mx-auto px-4 py-5 flex items-center justify-between">
      <h1 class="text-2xl font-bold">📦 Inventory Management</h1>
      <nav>
        <a href="/categories/new" class="bg-white text-indigo-600 px-4 py-2 rounded shadow hover:bg-indigo-50 transition">
          ➕ Add Category
        </a>
      </nav>
    </div>
  </header>

  <main class="max-w-4xl mx-auto px-4 py-10">
    <section>
      <h2 class="text-xl font-semibold mb-4">Categories</h2>
      <% if (locals.error) { %>
        <div class="max-w-2xl mx-auto mb-6">
          <div class="bg-red-200 text-red-800 px-4 py-3 rounded border border-red-400 shadow-md">
            <p class="text-sm font-medium"><%= error %></p>
          </div>
        </div>
      <% } %>
      <% if (categories.length === 0) { %>
        <p class="text-gray-600">No categories found.</p>
      <% } else { %>
        <ul class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <% categories.forEach(category => { %>
            <li class="bg-white shadow rounded-lg p-4 hover:shadow-md transition">
              <a href="/categories/<%= category.id %>/items" class="text-lg font-medium text-indigo-700 hover:underline">
                <%= category.name %>
              </a>
            </li>
          <% }); %>
        </ul>
      <% } %>
    </section>
  </main>
</body>
</html>
